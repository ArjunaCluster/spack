# This is the default user configuration when logging into Arjuna
# It it bootstrapped from the base CentOS Install
#
spack:
  # add package specs to the `specs` list
  specs:
  - gcc%gcc@4.8.5
  - python@3.9.6%gcc@4.8.5
  - py-pip
  concretization: together
  view: false
